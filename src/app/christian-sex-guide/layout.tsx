import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sex, Marriage & the Christian | Vine", description: "A biblical guide to sexuality in marriage — theology, purpose, purity, and healing from sexual brokenness." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
